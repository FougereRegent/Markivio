commit-msg:
  commands:
    "lint commit message":
      run: pnpm commitlint --edit {1}

pre-commit:
  parallel: true
  commands:
    backend:
      root: ./backend/api
      run: |
        dotnet format
    frontend:
      root: ./frontend/markivio-frontend
      run: |
        pnpm lint
