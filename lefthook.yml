commit-msg:
  commands:
    "lint commit message":
      run: pnpm commitlint --edit {1}

pre-commit:
  parallel: true
  jobs:
    - run: dotnet format
      root: ./backend/api/
      glob: "*.cs"
    - run:  pnpm lint
      root: ./frontend/markivio-frontend
