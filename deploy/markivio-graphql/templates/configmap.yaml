{{- if .Values.config -}} 
kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "deploy.fullname" .}}-configmap
  labels:
    {{- include "deploy.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value }}
{{- end }}
{{- end }}
